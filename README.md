# 📘 Book Web (Django + Docker)

## 🛠 Создание Docker-образа

Собрать Docker-образ из текущей директории:

```bash
docker build -t book-web ./
```

---

## ▶️ Запуск контейнера

Запуск Django-сервера в контейнере с пробросом порта 8000:

```bash
docker run -it -p 8000:8000 --name book-web-test -v $(pwd)/project:/app/project book-web python /app/project/manage.py runserver 0.0.0.0:8000
```

Сервер будет доступен на:

```text
http://localhost:8000
```
или

```text
http://<IP_ХОСТА>:8000
```

---

## 🔁 Повторный запуск контейнера

Если контейнер уже был создан и остановлен:

```bash
docker start book-web-instance
```

---

## 📄 Просмотр логов контейнера

Посмотреть логи работающего контейнера (например, вывод Django):

```bash
docker logs -f book-web-instance
```

---

## 🗑 Удаление контейнера

Если нужно удалить контейнер (например, перед пересозданием):

```bash
docker rm book-web-instance
```

> ⚠️ Контейнер должен быть остановлен перед удалением

При необходимости остановить контейнер:

```bash
docker stop book-web-instance
```

---



